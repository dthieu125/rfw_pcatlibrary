[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "robotframework-pcatlibrary"
version = "0.1.0"
description = "Robot Framework library for driving Qualcomm PCAT CLI workflows."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "PCAT Library Maintainers" }
]
license = "MIT"
keywords = ["robotframework", "pcat", "qualcomm", "automation", "cli"]
classifiers = [
  "Framework :: Robot Framework",
  "Framework :: Robot Framework :: Library",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Operating System :: OS Independent",
]
dependencies = [
  "robotframework>=6.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "ruff>=0.6",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
